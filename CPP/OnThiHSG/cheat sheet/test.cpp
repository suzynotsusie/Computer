// CPP program to illustrate
// std::to_string
#include <bits/stdc++.h>
using namespace std;
// Driver code
int main()
{
	string a[10] = {"chiu", "cuoc", "doi"};
	sort(a, a + 3);
	for (int i = 0; i < 6; i ++) cout << a[i] << " ";
	return 0;
}

