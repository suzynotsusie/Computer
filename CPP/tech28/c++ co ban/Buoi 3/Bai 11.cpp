#include <bits/stdc++.h>
using namespace std;

#define ll long long

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0); cout.tie(0);
	ll x, y;
	cin >> x >> y;
	cout << setprecision(0) << fixed <<  pow(x,y);
	return 0;
}
