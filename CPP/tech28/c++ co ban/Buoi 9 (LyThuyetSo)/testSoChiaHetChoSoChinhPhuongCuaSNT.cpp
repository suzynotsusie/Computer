#include <bits/stdc++.h>
using namespace std;

#define ll long long

void sang() {
	for (int i = 2; i <= maxn; i++) {
		if (n % i == 0) {
			int x = i * i;
			
			
		}
	}
}

int main() {
ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	ll n;
	cin >> n;
	ll maxn = sqrt(n);
	byte check[maxn + 5] = {};
		
	return 0;
}
